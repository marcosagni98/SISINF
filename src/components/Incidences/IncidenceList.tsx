import React from 'react';

const IncidenceList: React.FC = () => {
  return (
    <div>
      {/* Aquí va la tabla con datos de las incidiencias (hay que hacer que se pueda cambiar el contenido
        que se muestra en ella dependiendo del usuario, recibira un interface y solo se pintaran los datos 
        que vengan en el interface,pensar como hacerlo dinamico*/}
    </div>
  );
};

export default IncidenceList;
