/**
 * Interface representing the response data for a successful login
 * @interface LoginResponse
 * @property {string} token - The authentication token provided upon successful login
 */
export interface LoginResponse {
    token: string;
}