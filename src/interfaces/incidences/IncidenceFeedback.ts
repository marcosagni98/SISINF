/**
 * Interface representing the feedback provided for an incidence
 * @interface IncidenceFeedback
 * @property {string} feedback - The feedback text provided for the incidence
 * @property {number} rating - The rating given for the incidence
 */

export interface IncidenceFeedback {
    feedback: string;
    rating: number;
}