/**
 * Interface representing a generic response structure
 * @interface GenericResponse
 * @property {number} statusCode - The status code of the response
 * @property {string} message - The message associated with the response
 */

export interface GenericRespone {
    statusCode: number;
    message: string;
}