/**
 * Represents a generic API response structure containing a status code and message.
 * 
 * @interface Response
 * @property {string} improveDescription - A message providing additional information about the API response.
 */
export interface Response {
    improveDescription: string;
}