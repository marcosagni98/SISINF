/**
 * Interface representing the data required to update a user's information
 * @interface UpdateUser
 * @property {number} id - The unique identifier of the user to be updated
 */

export interface UpdateUser {
    id: number;
  }
  